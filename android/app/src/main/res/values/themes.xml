<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- GeneralsX @feature Android port launcher-ui-2026 08/09/2026 The
         launcher's Material 3 theme, applied per-Activity in
         AndroidManifest.xml (Setup / Folder Picker / Log Viewer /
         GeneralsOnline). The application-level Theme.NoTitleBar.Fullscreen
         stays the default for GameActivity (the game surface), which
         must never pick any of this up.

         Deliberately Theme.Material3.Dark rather than .DayNight: this
         launcher has always drawn on a dark ground, its accent palette is
         designed for one, and committing to it means the contrast of every
         colour pair below can be reasoned about once instead of twice. -->
    <style name="Theme.Generals" parent="Theme.Material3.Dark.NoActionBar">
        <!-- M3 colour roles -->
        <item name="colorPrimary">@color/gen_primary</item>
        <item name="colorOnPrimary">@color/gen_on_primary</item>
        <item name="colorPrimaryContainer">@color/gen_primary_container</item>
        <item name="colorOnPrimaryContainer">@color/gen_on_primary_container</item>
        <item name="colorSecondary">@color/gen_secondary</item>
        <item name="colorOnSecondary">@color/gen_on_secondary</item>
        <item name="colorSecondaryContainer">@color/gen_secondary_container</item>
        <item name="colorOnSecondaryContainer">@color/gen_on_secondary_container</item>
        <item name="colorTertiary">@color/gen_tertiary</item>
        <item name="colorOnTertiary">@color/gen_on_tertiary</item>
        <item name="colorTertiaryContainer">@color/gen_tertiary_container</item>
        <item name="colorOnTertiaryContainer">@color/gen_on_tertiary_container</item>
        <item name="colorError">@color/gen_error</item>
        <item name="colorOnError">@color/gen_on_error</item>
        <item name="colorErrorContainer">@color/gen_error_container</item>
        <item name="colorOnErrorContainer">@color/gen_on_error_container</item>
        <item name="colorSurface">@color/gen_surface</item>
        <item name="colorOnSurface">@color/gen_on_surface</item>
        <item name="colorSurfaceVariant">@color/gen_surface_container_high</item>
        <item name="colorOnSurfaceVariant">@color/gen_on_surface_variant</item>
        <item name="colorOutline">@color/gen_outline</item>
        <item name="android:colorBackground">@color/gen_background</item>
        <item name="colorOnBackground">@color/gen_on_surface</item>

        <!-- Framework slots the plain android.* widgets (AlertDialog, Toast
             anchors, ListView, EditText) still read. -->
        <item name="colorPrimaryDark">@color/gen_primary_dark</item>
        <item name="colorAccent">@color/gen_accent</item>
        <item name="android:windowBackground">@color/gen_background</item>
        <item name="android:textColorPrimary">@color/gen_on_surface</item>
        <item name="android:textColorSecondary">@color/gen_on_surface_variant</item>
        <item name="android:textColorTertiary">@color/gen_on_surface_faint</item>
        <item name="android:textColorHint">@color/gen_on_surface_faint</item>
        <item name="android:textColorHighlight">@color/gen_primary_container</item>

        <!-- Edge-to-edge: targetSdk 35 draws behind the bars anyway, and
             InsetUtil.applySafeInsets() pads the content out from under
             them. Painting the bars transparent lets the page colour run
             all the way to the physical edges instead of banding. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Material's own dialogs/alerts follow the same palette. -->
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.Generals.Dialog</item>
        <item name="android:alertDialogTheme">@style/Theme.Generals.AlertDialog</item>
    </style>

    <style name="ThemeOverlay.Generals.Dialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="colorSurface">@color/gen_surface_container_high</item>
        <item name="colorPrimary">@color/gen_primary</item>
        <item name="android:textColorPrimary">@color/gen_on_surface</item>
        <item name="android:textColorSecondary">@color/gen_on_surface_variant</item>
    </style>

    <!-- SetupActivity/LogViewerActivity still build a couple of plain
         android.app.AlertDialogs (single-choice language / render-backend
         pickers, "clear logs?"). Give those the same ground and text
         colours so they don't flash a light-grey Holo box on a near-black
         screen. -->
    <style name="Theme.Generals.AlertDialog" parent="android:Theme.Material.Dialog.Alert">
        <item name="android:windowBackground">@drawable/gen_dialog_background</item>
        <item name="android:colorAccent">@color/gen_primary</item>
        <item name="android:textColorPrimary">@color/gen_on_surface</item>
        <item name="android:textColorSecondary">@color/gen_on_surface_variant</item>
        <item name="android:textColorAlertDialogListItem">@color/gen_on_surface</item>
    </style>

    <!-- Applied with a ContextThemeWrapper where an outlined text field is
         wanted: TextInputLayout resolves its style from ?attr/textInputStyle,
         so swapping that attribute is the way to pick OutlinedBox for a
         view built in code rather than inflated from XML. -->
    <style name="ThemeOverlay.Generals.OutlinedField" parent="">
        <item name="textInputStyle">@style/Widget.Material3.TextInputLayout.OutlinedBox</item>
    </style>
</resources>
